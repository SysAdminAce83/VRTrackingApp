@model IEnumerable<VRTrackingApp.Data.Models.Policy>
@using VRTrackingApp.Data.Models
@{
    ViewData["Title"] = "Policies";
    Layout = "_Layout";
}

<div class="container-fluid px-4">
    <h1 class="mt-4">Policies</h1>
    <ol class="breadcrumb mb-4">
        <li class="breadcrumb-item"><a asp-action="Index" asp-controller="Dashboard">Dashboard</a></li>
        <li class="breadcrumb-item"><a asp-action="Index" asp-controller="Policy">Policies</a></li>
        <li class="breadcrumb-item active">List</li>
    </ol>

    <div class="row mb-3">
        <div class="col-md-4">
            <a asp-action="Create" class="btn btn-primary btn-sm">Create New Policy</a>
        </div>
        <div class="col-md-8 text-end">
            <form asp-action="Index" method="get" class="row g-2">
                <div class="col-md-3">
                    <input type="text" name="title" class="form-control form-control-sm" placeholder="Policy title" value="@ViewBag.Title" />
                </div>
                <div class="col-md-3">
                    <input type="text" name="status" class="form-control form-control-sm" placeholder="Status" value="@ViewBag.Status" />
                </div>
                <div class="col-md-2">
                    <button type="submit" class="btn btn-sm btn-primary">Filter</button>
                </div>
                <div class="col-md-2">
                    <a asp-action="Index" class="btn btn-sm btn-outline-secondary">Clear</a>
                </div>
            </form>
        </div>
    </div>

    <div class="card">
        <div class="card-header">
            <i class="fas fa-file-alt"></i> Policy List
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead>
                        <tr>
                            <th>Title</th>
                            <th>Description</th>
                            <th>Category</th>
                            <th>Version</th>
                            <th>Effective Date</th>
                            <th>Review Date</th>
                            <th>Owner</th>
                            <th>Status</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var policy in Model)
                        {
                            <tr>
                                <td>@policy.Title</td>
                                <td class="small" style="max-width:200px;">@policy.Description</td>
                                <td>@policy.Category</td>
                                <td>@policy.Version</td>
                                <td>@policy.EffectiveDate.ToString("yyyy-MM-dd")</td>
                                <td>@policy.ReviewDate.ToString("yyyy-MM-dd")</td>
                                <td>@(policy.Owner?.UserName ?? "Unassigned")</td>
                                <td>
                                    <span class="badge bg-@(policy.Status == "Draft" ? "secondary" : policy.Status == "Under Review" ? "warning" : policy.Status == "Approved" ? "success" : "danger")">
                                        @policy.Status
                                    </span>
                                </td>
                                <td>
                                    <a asp-action="Details" asp-route-id="@policy.Id" class="btn btn-sm btn-outline-primary">Details</a>
                                    <a asp-action="Edit" asp-route-id="@policy.Id" class="btn btn-sm btn-outline-secondary">Edit</a>
                                    <a asp-action="Delete" asp-route-id="@policy.Id" class="btn btn-sm btn-outline-danger">Delete</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
