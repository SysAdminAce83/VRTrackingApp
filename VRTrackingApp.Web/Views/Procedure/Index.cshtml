@model IEnumerable<VRTrackingApp.Data.Models.Procedure>
@using VRTrackingApp.Data.Models
@{
    ViewData["Title"] = "Procedures";
    Layout = "_Layout";
}

<div class="container-fluid px-4">
    <h1 class="mt-4">Procedures</h1>
    <ol class="breadcrumb mb-4">
        <li class="breadcrumb-item"><a asp-action="Index" asp-controller="Dashboard">Dashboard</a></li>
        <li class="breadcrumb-item"><a asp-action="Index" asp-controller="Procedure">Procedures</a></li>
        <li class="breadcrumb-item active">List</li>
    </ol>

    <div class="row mb-3">
        <div class="col-md-4">
            <a asp-action="Create" class="btn btn-primary btn-sm">Create New Procedure</a>
        </div>
        <div class="col-md-8 text-end">
            <form asp-action="Index" method="get" class="row g-2">
                <div class="col-md-3">
                    <input type="text" name="title" class="form-control form-control-sm" placeholder="Procedure title" value="@ViewBag.Title" />
                </div>
                <div class="col-md-3">
                    <input type="text" name="status" class="form-control form-control-sm" placeholder="Status" value="@ViewBag.Status" />
                </div>
                <div class="col-md-2">
                    <button type="submit" class="btn btn-sm btn-primary">Filter</button>
                </div>
                <div class="col-md-2">
                    <a asp-action="Index" class="btn btn-sm btn-outline-secondary">Clear</a>
                </div>
            </form>
        </div>
    </div>

    <div class="card">
        <div class="card-header">
            <i class="fas fa-list-ol"></i> Procedure List
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead>
                        <tr>
                            <th>Title</th>
                            <th>Description</th>
                            <th>Standard</th>
                            <th>Policy</th>
                            <th>Version</th>
                            <th>Effective Date</th>
                            <th>Review Date</th>
                            <th>Owner</th>
                            <th>Status</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var procedure in Model)
                        {
                            <tr>
                                <td>@procedure.Title</td>
                                <td class="small" style="max-width:200px;">@procedure.Description</td>
                                <td>@procedure.Standard.Title</td>
                                <td>@procedure.Standard.Policy.Title</td>
                                <td>@procedure.Version</td>
                                <td>@procedure.EffectiveDate.ToString("yyyy-MM-dd")</td>
                                <td>@procedure.ReviewDate.ToString("yyyy-MM-dd")</td>
                                <td>@(procedure.Owner?.UserName ?? "Unassigned")</td>
                                <td>
                                    <span class="badge bg-@(procedure.Status == "Draft" ? "secondary" : procedure.Status == "Under Review" ? "warning" : procedure.Status == "Approved" ? "success" : "danger")">
                                        @procedure.Status
                                    </span>
                                </td>
                                <td>
                                    <a asp-action="Details" asp-route-id="@procedure.Id" class="btn btn-sm btn-outline-primary">Details</a>
                                    <a asp-action="Edit" asp-route-id="@procedure.Id" class="btn btn-sm btn-outline-secondary">Edit</a>
                                    <a asp-action="Delete" asp-route-id="@procedure.Id" class="btn btn-sm btn-outline-danger">Delete</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
